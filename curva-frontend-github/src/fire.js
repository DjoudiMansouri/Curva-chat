import firebase from 'firebase'
const firebaseConfig = {
    //FirebaseConfig infos goes in here / Les informations de configuration de Firebase sont placées ici
}
const fire = firebase.initializeApp(firebaseConfig)
export default fire